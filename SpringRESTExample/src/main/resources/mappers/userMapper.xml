<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kitchen.mapper.UserMapper">

	<!-- 이거에 접근할 때 namespace.id 로 접근한다. -->
	<!-- resultType은 자바의 타입과 매핑되는 것이 있다. 여기선 string이 java.lang.String과 매핑 -->
	
	<insert id="register">
		insert into users(id, name, password)
		values( #{id} , #{name}, #{password} );
	</insert>

	<select id="get" resultType="com.kitchen.model.User">
		select *
		from users
		where id = #{id};
	
	</select>
	
	<select id="getList" resultType="com.kitchen.model.User">
		select *
		from users;
	</select>
	
	<select id="delete">
		delete *
		from users
		where id = #{id}
	</select>
	
</mapper>
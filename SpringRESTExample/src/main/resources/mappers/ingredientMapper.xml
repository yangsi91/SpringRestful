<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kitchen.mapper.IngredientMapper">

	<!-- 이거에 접근할 때 namespace.id 로 접근한다. -->
	<!-- resultType은 자바의 타입과 매핑되는 것이 있다. 여기선 string이 java.lang.String과 매핑 -->
	
	<insert id="insert">
		insert into ingredient(kor_name, eng_names, img_location, type_id, created)
		values( #{kor_name} , #{eng_names}, #{img_location}, #{type_id}, now() );
	</insert>

	<select id="getIngredient" resultType="com.kitchen.model.Ingredient">
		select *
		from users
		where ingredient_id = #{ingredient_id};
	
	</select>
	
	<select id="getList" resultType="com.kitchen.model.Ingredient">
		select *
		from ingredient;
	</select>
	
	<select id="getListByType" resultType="com.kitchen.model.Ingredient">
		select *
		from ingredient
		where type_id=#{type_id};
	</select>
	
	
	<delete id="delete" parameterType="int">
		delete from ingredient where ingredient_id = #{ingredient_id};
	</delete>
	
</mapper>
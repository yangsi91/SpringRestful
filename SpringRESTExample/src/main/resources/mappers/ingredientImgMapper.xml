<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kitchen.mapper.IngredientImgMapper">

	<!-- 이거에 접근할 때 namespace.id 로 접근한다. -->
	<!-- resultType은 자바의 타입과 매핑되는 것이 있다. 여기선 string이 java.lang.String과 매핑 -->
	
	<insert id="insert">
		insert into ingredient_img(ingredient_id, basecode, tags, created)
		values( #{ingredient_id} , #{basecode}, #{tags}, now() );
	</insert>

	<select id="getData" resultType="com.kitchen.model.IngredientImg">
		select *
		from ingredient_img
		where img_id = #{img_id};
	
	</select>
	
	<select id="getList" resultType="com.kitchen.model.IngredientImg">
		select *
		from ingredient_img;
	</select>
	
	<delete id="delete" parameterType="int">
		delete from ingredient_img where img_id = #{img_id};
	</delete>
	
</mapper>
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kitchen.mapper.RecipeDetailsMapper">

	<!-- 이거에 접근할 때 namespace.id 로 접근한다. -->
	<!-- resultType은 자바의 타입과 매핑되는 것이 있다. 여기선 string이 java.lang.String과 매핑 -->
	
	<insert id="insert" parameterType="com.kitchen.model.RecipeDetails" useGeneratedKeys="true"   
  keyProperty="details_id">
		insert into recipe_details(recipe_id, step, contents, created)
		values( #{recipe_id} , #{step}, #{contents}, now() );
	</insert>

	<select id="getData" resultType="com.kitchen.model.RecipeDetails">
		select *
		from recipe_details
		where details_id = #{details_id};
	</select>
	
	<select id="getList" resultType="com.kitchen.model.RecipeDetails">
		select *
		from recipe_details;
	</select>
	
	<delete id="delete" parameterType="int">
		delete from recipe_details where details_id = #{details_id};
	</delete>
	
</mapper>